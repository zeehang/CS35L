hello this is a shorter file hello
break will exit the loop completely, continue will just skip the current iteration.
break will exit the loop completely, continue will just skip the current iteration.
break will exit the loop completely, continue will just skip the current iteration.
break will exit the loop completely, continue will just skip the current iteration.break will exit the loop completely, continue will just skip the current iteration.break will exit the loop completely, continue will just skip the current iteration.break will exit the loop completely, continue will just skip the current iteration.
